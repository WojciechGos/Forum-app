<div class="container">
    <form method="POST" id="postForm" enctype="multipart/form-data">
        <input id="inputFile" type="file">
        <div class="mb-3 flex-fill">
            <label for="inputContent" class="form-label">Treść</label>
            <div class="custom-input" 
            id="createPostForm" 
            contenteditable="true"
            name="content">

                <div><br></div>
                <div>
                    <img src="https://interactive-examples.mdn.mozilla.net/media/cc0-images/grapefruit-slice-332-332.jpg"
                       >
                       123<br>
                </div>
                <div><br></div>
                <div><br></div>
                <div><br></div>
            
                <div><br></div>
                <div><br></div>
                <div><img src="https://www.industrialempathy.com/img/remote/ZiClJf-1920w.jpg"
                        ><br></div>
                <div>
                    <img src="https://4rooms.com.pl/userdata/public/gfx/1279cded36c5a39ffac5a8b86ffa654c.jpg" alt="">
                </div>
            </div>
        </div>
        <button type="submit" class="btn btn-primary" id="createPost">Dodaj Post</button>
    </form>


</div>


<script>
    const editableDiv = document.getElementById('createPostForm')
    const input = document.getElementById('inputFile')
    const form = document.getElementById('postForm')
    
    function generateID(){
        return Date.now()
    }

    function getImages(){
        let collection = editableDiv.getElementsByTagName('div')
        let images = []
        for(let item of collection){
            let tmp = item.getElementsByTagName('img')
            if(tmp.length != 0){
                images.push(tmp[0].currentSrc)
            }
        }
        console.log(images)
        return images
    }


    form.addEventListener('submit', function(event){
        event.preventDefault()
        let formData = new FormData()
        // security problem 
        // replace innerHTML or smt
        formData.append('data', editableDiv.innerHTML)
       
        console.log(formData.values)

        fetch("entry", {
            method: 'post',
            body: formData
        }).then(response =>{
            return response.text()
        }).then(text =>{
            console.log(text)
        }).catch(error=>{
            console.error(error)
        })
    })


</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-U1DAWAznBHeqEIlVSCgzq+c9gqGAJn5c/t99JyeKa9xxaYpSvHU5awsuZVVFIhvj"
    crossorigin="anonymous"></script>